---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { config } from '../../config';
import { getCollection } from 'astro:content';

const allServices = await getCollection('services');
const services = allServices.sort((a, b) => a.data.order - b.data.order);
---

<BaseLayout
  title="Our Services"
  description={`Tax preparation, bookkeeping, payroll, and consulting services from ${config.business.name} in ${config.contact.address.city}, ${config.contact.address.state}.`}
  schemaType="service"
  breadcrumbs={[{ name: 'Home', url: '/' }, { name: 'Services', url: '/services' }]}
>
  <!-- HERO -->
  <section class="py-20 lg:py-28" style={`background: var(--color-secondary);`}>
    <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 text-center">
      <p class="section-label mb-4">What We Do</p>
      <h1 class="text-4xl sm:text-5xl font-bold mb-6" style="font-family: var(--font-family-display); color: var(--color-text-on-dark);">
        Our Services
      </h1>
      <p class="text-lg" style="color: var(--color-text-on-dark-muted);">
        Comprehensive tax, accounting, and financial services for individuals and businesses in {config.contact.address.city}, Ohio and the surrounding area.
      </p>
    </div>
  </section>

  <!-- SERVICES GRID -->
  <section class="py-16 lg:py-24" style="background: var(--color-bg);">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        {services.map((svc, i) => (
          <a
            href={`/services/${svc.data.slug}`}
            class="group rounded-lg overflow-hidden transition-shadow duration-300 reveal"
            style={`background: var(--color-surface); box-shadow: var(--shadow-card); transition-delay: ${i * 100}ms;`}
          >
            <div class="aspect-[16/10] overflow-hidden">
              <img
                src={svc.data.heroImage}
                alt={svc.data.heroAlt}
                class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                width="600"
                height="375"
                loading="lazy"
              />
            </div>
            <div class="p-6">
              <h2 class="text-xl font-bold mb-2" style="font-family: var(--font-family-display);">{svc.data.title}</h2>
              <p class="text-sm mb-4" style="color: var(--color-text-muted);">{svc.data.shortDescription}</p>
              <span class="text-sm font-bold" style="color: var(--color-primary);">Learn More &rarr;</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-16 text-center" style={`background: var(--color-dark);`}>
    <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold mb-4" style="font-family: var(--font-family-display); color: var(--color-text-on-dark);">
        Not Sure Which Service You Need?
      </h2>
      <p class="mb-8" style="color: var(--color-text-on-dark-muted);">
        Contact us today and we'll help you find the right solution for your situation.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href={config.cta.primary.href} class="btn-primary">{config.cta.primary.label}</a>
        <a href={`tel:${config.contact.phoneRaw}`} class="btn-ghost">{config.cta.phone.label}</a>
      </div>
    </div>
  </section>
</BaseLayout>
